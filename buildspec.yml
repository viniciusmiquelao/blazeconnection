version: 0.2

phases:
  pre_build:
    - echo Install packages...
    - yarn
  build:
    - echo Create Build...
    - yarn build
  post_build:
    - echo Removing unnecessary files...
    - rm -rf node_modules
artifacts:
  base-directory: "."
  files:
    - "**/*"
